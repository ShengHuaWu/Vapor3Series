#set("content") {
    <table class="table table-bordered table-hove">
        <thead class="thread-light">
            <tr>
                <th>Name</th>
                <th>Username</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <th>#(user.name)</th>
                <th>#(user.username)</th>
            </tr>
        </tbody>
    </table>
}

#embed("base")
